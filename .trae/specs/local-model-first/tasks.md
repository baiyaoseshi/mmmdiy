# Tasks

- [x] Task 1: 数据模型变更 — `AI配置模型.cs`
  - [x] `AIConfigData` 新增 `是否优先本地`（默认 `true`）
  - [x] `AIPersistenceData` 移除 `评审AI配置Id`、`微调后评审模型名`，新增 `经验AI配置Id`
  - [x] `执行数据迁移()` 中忽略旧字段

- [x] Task 2: 移除旧代码 — `AI使用经验管理器.cs`
  - [x] 移除：`构建经验系统消息`/`评审计划`/`执行微调`/`导出微调数据`/`查找历史相似案例`
  - [x] 移除：`触发经验总结`/`更新工具经验`/`清空工具经验`/`获取工具经验`/`获取经验状态`
  - [x] 移除：`加载经验`/`保存经验`、`经验存储根`/`工具经验条目` 数据类
  - [x] 保留：`计划节点`/`工具调用记录`/`统计存储根`/`追加调用记录`/`从回复中提取计划`/`解析计划`

- [x] Task 3: 移除旧代码 — `AI配置管理器.cs`
  - [x] 移除：`构建经验系统消息从历史`/`获取评审AI配置`/`提取工具ID从消息历史`
  - [x] 移除：`工具在计划内`/`查找计划叶子工具`
  - [x] 移除 `构建消息列表`/`构建提示词` 中经验注入代码
  - [x] 移除 `调用本地/远程AI流式带工具` 中评审AI代码 + 计划校验

- [x] Task 4: 移除 UI 触发 — `AIChatPage.xaml.cs` + `MainWindowViewModel.cs`
  - [x] 移除 `_本轮新消息数` 及计数逻辑
  - [x] 移除 IsAIPage setter 中经验总结触发

- [x] Task 5: 清理 MCPToolDefinition — `MCP工具管理器.cs`
  - [x] 移除 `已有使用经验`/`经验更新时间`，保留 `已有统计数据`
  - [x] `加载工具列表()` 移除填充经验状态代码

- [x] Task 6: 脚本 GUID 身份 — `自动任务脚本.cs`
  - [x] 新增 `脚本Id` 字段（`Guid.NewGuid().ToString("N")`）
  - [x] `保存为字符串()` 头部输出 `脚本Id[GUID]`
  - [x] `解析(string)` 解析脚本Id，旧格式自动生成

- [x] Task 7: 工具ID GUID 派生 — `MCP工具管理器.cs`
  - [x] `加载工具列表()` 用 `tool_` + GUID 前12位生成工具ID
  - [x] `MCPToolDefinition` 新增 `脚本Id` 字段

- [x] Task 8: 印象数据模型 — `AI使用经验管理器.cs`
  - [x] `工具印象` 类：工具ID、第一印象、当前印象、置信度、首次使用时间、最近更新时间、修正历史
  - [x] `印象修正记录` 类：类型（第一印象/修正/覆盖）、旧印象、新印象、触发原因、来源对话ID、时间戳
  - [x] `印象存储根` 类：`Dictionary<string, 工具印象>`
  - [x] `加载印象()`/`保存印象()` — 读写 `ai_tool_impressions.json`，线程安全锁

- [x] Task 9: 印象管理核心方法 — `AI使用经验管理器.cs`
  - [x] `获取工具印象(工具ID)` → 有印象返回现有，无印象返回基于名称+备注的初始印象
  - [x] `生成第一印象(经验配置, 工具调用记录)` → 调用经验AI生成
  - [x] `更新印象(工具ID, impression_match, 评估原因, 工具调用记录)` → 一致/偏差/矛盾三类处理
  - [x] `清空工具印象`/`清空所有印象`/`获取工具印象状态`

- [x] Task 10: 工具描述融入印象 — `MCP工具管理器.cs`
  - [x] `构建工具定义列表()` 中，对每个工具查询共享印象
  - [x] 置信度 ≥ 0.5 → 描述 += `【印象】当前印象（置信度: X%）`
  - [x] 置信度 < 0.5（仅初始印象）→ 不增强，保持原始描述
  - [x] 此逻辑对**所有 AI 调用路径**生效

- [x] Task 11: 经验模型评估 — `AI使用经验管理器.cs`
  - [x] `评估工具调用(经验配置, 工具调用记录)` — 构建评估 Prompt 含当前印象
  - [x] 输出 `{"quality", "impression_match", "reason", "tags"}`，30 秒超时

- [x] Task 12: 训练数据管理 — `AI使用经验管理器.cs`
  - [x] `追加训练样本(工具调用记录, quality, impression_match, string 当前印象, int 印象版本)` — 基于 `工具调用记录` 扩展写入 JSONL
  - [x] **input 以印象为主体**：格式 `"工具印象: [印象] | 输入参数: {...} | 当前计划: [计划树扁平化]"`，不含用户名称/备注
  - [x] 保留原始记录的完整字段：`调用时计划树`、`调用后树修改`、`是否成功`、`耗时ms`
  - [x] 新增字段：`quality`、`impression_match`、`工具GUID`、`印象版本`
  - [x] 初始印象（置信度 0.3）的调用不入训练数据
  - [x] `数据清洗()`/`构建评测集()`/`获取训练数据统计()`

- [x] Task 13: 模型训练管线 — `AI使用经验管理器.cs`
  - [x] `执行SFT微调`/`执行DPO优化`/`执行RL强化` — TODO 占位
  - [x] `执行完整训练管线()` — 串联：数据清洗 → 评测集 → SFT（≥50条）→ DPO → RL，返回进度回调

- [x] Task 14: 训练触发与 AI 锁定 — `MainWindowViewModel.cs` + `AI配置管理器.cs`
  - [x] `MainWindowViewModel` 新增 `_训练倒计时定时器`（5 分钟 `System.Timers.Timer`）
  - [x] 窗口 Hide（托盘）→ 启动倒计时；窗口 Show → 取消倒计时
  - [x] 倒计时到期 → 触发 `执行完整训练管线()`，通过进度回调更新 UI
  - [x] `AI配置管理器` 新增 `是否训练中` 静态属性（`volatile bool`）
  - [x] `调用AI流式带工具` 入口检查 `是否训练中` → 若 true 立即返回"模型训练中，请稍候再试"
  - [x] 训练完成/失败 → 重置 `是否训练中 = false`
  - [x] `MainWindowViewModel` 新增 `训练进度` 属性（当前阶段 + 百分比）供 UI 绑定

- [x] Task 15: 本地优先路由 — `AI配置管理器.cs`
  - [x] `解析提供者(AIConfigData)` — 本地优先 → API 降级
  - [x] 修改 `调用AI`/`调用AI流式`/`调用AI分析截图` 使用新路由

- [x] Task 16: 经验 AI 配置 — `AI配置管理器.cs`
  - [x] `获取经验AI配置()`/`更新经验AI配置()`

- [x] Task 17: 集成评估+印象到工具调用链 — `AI配置管理器.cs`
  - [x] 工具执行后 fire-and-forget：评估 → 印象更新 → 训练样本
  - [x] 移除旧的 `自动训练检查()` 独立调用（已由 Task 14 的托盘触发和手动触发取代）

- [x] Task 18: UI 变更 — `GlobalAISettings`
  - [x] 移除评审AI选择器，新增经验AI配置下拉 + `是否优先本地` 开关
  - [x] 新增"开始训练""数据清洗""构建评测集""清空印象""查看印象"按钮
  - [x] 新增训练进度显示区域（当前阶段、已用时间、进度条）

- [x] Task 19: 编译验证
  - [x] `dotnet build` 工具库 0 错误
  - [x] `dotnet build` 用户界面 0 错误
