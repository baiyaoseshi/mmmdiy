# Tasks

- [x] Task 1: 创建数据模型 — 计划树节点、工具调用记录、经验数据结构
  - [x] 在 `AI使用经验管理器.cs` 中定义 `计划节点`（含需求、工具ID、子步骤列表）
  - [x] 定义 `工具调用记录`（含10个字段）
  - [x] 定义 `工具经验条目`（含规则、更新时间）
  - [x] 定义 `经验存储根`（`Dictionary<string, 工具经验条目>`）
  - [x] 定义 `统计存储根`（`List<工具调用记录>`）

- [x] Task 2: 创建 `AI使用经验管理器.cs` 核心 — 读写、清空、导出
  - [x] 实现 `加载经验()` / `保存经验()` — 读写 `ai_experience.json`
  - [x] 实现 `加载统计()` / `保存统计()` — 读写 `ai_tool_statistics.json`
  - [x] 实现 `追加调用记录(工具调用记录)` — 线程安全追加
  - [x] 实现 `更新工具经验(工具ID, 规则)` — 覆盖或新增
  - [x] 实现 `清空工具经验(工具ID)`、`清空所有经验()`、`清空工具统计(工具ID)`
  - [x] 实现 `获取经验状态(工具ID)` → 返回 `(是否有经验, 更新时间, 是否有统计)`
  - [x] 实现 `导出微调数据()` → JSONL 格式

- [x] Task 3: 计划解析器 — `[计划]...[/计划]` 文本解析为计划树
  - [x] 实现 `解析计划(string)` → `计划节点`
  - [x] 支持叶子步骤格式 `需求|工具ID`
  - [x] 支持内部步骤格式 `需求|*|子需求1|子工具1|...`
  - [x] 支持递归嵌套

- [x] Task 4: 修改 `MCP工具定义` — 新增 3 个经验状态属性
  - [x] `MCPToolDefinition` 新增 `已有使用经验` (bool)、`经验更新时间` (string)、`已有统计数据` (bool)

- [x] Task 5: 修改 `MCP工具管理器.cs` — 加载时填充状态 + 执行时写记录
  - [x] `加载工具列表()` 中为每个工具填充 3 个经验状态属性
  - [x] `执行工具()` 中构建 `工具调用记录` 并调用 `追加调用记录()`
  - [x] `执行工具()` 签名扩展：新增可选的 `对话ID`、`对话轮次`、`计划树`、`树修改` 参数

- [x] Task 6: 修改 `AI配置管理器.cs` — 经验注入 + 计划校验 + 评审 + 计划树跟踪
  - [x] `构建消息列表()` 和 `构建提示词()` 中插入经验 system message
  - [x] `调用AI流式带工具` 签名扩展：新增可选 `计划树` 参数
  - [x] 实现 `从回复中提取计划(string 回复)` — 提取 `[计划]...[/计划]` 块
  - [x] 实现懒校验逻辑：有 tool_call 时检查计划是否存在、工具是否在计划内
  - [x] 实现 `评审计划(计划节点, 工具列表)` 调用评审 AI（Phase A 上下文学习）
  - [x] 工具调用前注入当前计划树，工具调用后记录树修改

- [x] Task 7: 修改 `AI配置模型.cs` — 新增评审AI配置字段
  - [x] `AIPersistenceData` 新增 `评审AI配置` (AIConfigData)

- [x] Task 8: 修改 `AIChatPage.xaml.cs` — 新消息计数 + 暴露状态
  - [x] 新增 `_本轮新消息数` 字段，每次 AI 回复完成后递增
  - [x] 进入 AI 页面时重置计数
  - [x] 暴露 `有未总结消息` 属性或事件

- [x] Task 9: 修改 `MainWindowViewModel.cs` — 离开 AI 页面触发总结
  - [x] `IsAIPage` setter 中检测：从 true → false 且有新消息 → 触发后台总结
  - [x] 调用 `AI使用经验管理器` 的总结流程

- [x] Task 10: 编译验证
  - [x] `dotnet build` 工具库项目 ✅
  - [x] `dotnet build` 用户界面项目 ✅

- [x] Task 11（Phase B）: 自动微调评审 AI
  - [x] `导出微调数据()` — 将原始记录导出为 instruction/input/output JSONL
  - [x] `执行微调()` — 自动微调流程（基于经验构建增强 system prompt 创建 Ollama 模型），TODO 注释清晰以便用户覆盖
  - [x] `AIPersistenceData` 新增 `微调后评审模型名` 字段
  - [x] 微调触发条件：新增记录 ≥ 20 条或经验写入后
  - [x] 评审时若 `微调后评审模型名` 存在则优先使用

# Task Dependencies
- Task 2 依赖 Task 1
- Task 3 独立，可与 Task 1/2 并行
- Task 4 独立
- Task 5 依赖 Task 2、Task 4
- Task 6 依赖 Task 2、Task 3、Task 5
- Task 7 独立
- Task 8 依赖 Task 6（需要传计划树等新参数）
- Task 9 依赖 Task 2、Task 8
- Task 10 依赖 Task 2-9