# Checklist

- [x] `AI使用经验管理器.cs` 存在，包含所有公开方法
- [x] `ai_experience.json` 正确读写，格式与 spec 一致
- [x] `ai_tool_statistics.json` 正确读写，格式包含全部 10 个字段
- [x] `清空工具经验()` 正确清空指定工具的经验+统计
- [x] `清空所有经验()` 正确清空全部经验+统计
- [x] `清空工具统计()` 仅清空统计，保留经验规则
- [x] `导出微调数据()` 输出合法 JSONL 格式
- [x] `解析计划()` 正确解析叶子步骤和内部步骤（含嵌套）
- [x] `MCPToolDefinition` 包含 `已有使用经验`、`经验更新时间`、`已有统计数据` 三个属性
- [x] `加载工具列表()` 正确填充 3 个经验状态属性
- [x] `执行工具()` 正确写入原始调用记录（含计划树字段）
- [x] 构建消息时正确注入经验 system message（有经验时）
- [x] 构建消息时跳过注入（无经验时）
- [x] 无工具调用时不触发计划校验
- [x] 有工具调用但无计划时返回错误信息
- [x] 工具不在计划内时返回错误信息
- [x] 计划格式正确时解析通过，从回复中提取计划树
- [x] `AIPersistenceData` 包含 `评审AI配置` 字段
- [x] 配置了评审 AI 时调用评审流程
- [x] 未配置评审 AI 时跳过评审
- [x] `AIChatPage` 正确跟踪新消息计数
- [x] 离开 AI 页面 + 有新消息时触发后台总结
- [x] 离开 AI 页面 + 无新消息时不触发总结
- [x] AI 回复"无新经验"时不修改 `ai_experience.json`
- [x] 工具库项目编译通过
- [x] 用户界面项目编译通过
- [x] `导出微调数据()` 输出合法 JSONL（含 instruction/input/output 三元组）
- [x] `执行微调()` 有清晰的 TODO 注释，逻辑可被用户覆盖
- [x] `AIPersistenceData` 包含 `微调后评审模型名` 字段
- [x] 自动微调在达到阈值时触发
- [x] 评审 AI 优先使用微调后模型名（若存在）
