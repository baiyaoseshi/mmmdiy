# Checklist

## AI配置管理器
- [x] `按名称查找对话(string)` 方法存在且正确匹配名称

## 等待管理器
- [x] `等待类型.AI对话完成等待` 枚举值已添加
- [x] `创建AI对话完成等待()` 方法正确实现
- [x] `通知AI回复完成()` 方法正确匹配目标对话Id并触发等待
- [x] 超时机制与现有时间/事件等待一致
- [x] 同名等待互斥（取消旧等）

## 通用 MCP 工具
- [x] `wait_for_ai_reply` 工具定义在 `获取通用工具列表()` 中
- [x] 工具 ID 在 `内置工具ID列表` 中
- [x] `执行内置工具()` switch 包含 case
- [x] 参数验证：`对话名` 必填
- [x] 目标对话不存在时返回错误
- [x] 成功启动等待时返回确认信息

## AIChatPage 通知
- [x] 普通发送路径：`完成AI回复()` 非空后调用 `通知AI回复完成`
- [x] `/spec` 等待回调路径：`完成AI回复()` 非空后调用 `通知AI回复完成`
- [x] 用户取消/异常中断时**不**通知（仅自然完成才通知）

## 编译
- [x] `dotnet build` 工具库通过
- [x] `dotnet build` 用户界面通过
