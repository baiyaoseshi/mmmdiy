# Checklist

- [x] 发送按钮在 AI 输出期间显示红色"停止"，正常时显示蓝色"发送"
- [x] 点击"停止"按钮能取消正在进行的 AI 流式请求
- [x] 按 Enter 键在 AI 输出期间也能触发打断
- [x] AI 被打断后，已输出的部分内容保留在对话中
- [x] AI 被打断后显示系统消息"⏹ AI 回复已被用户中止"
- [x] AI 被打断后输入框和按钮恢复正常状态
- [x] `OperationCanceledException` 被正确捕获，不会作为未处理异常弹出
- [x] 打断后可以立即发送新消息（不会残留旧的 CancellationToken）
- [x] 本地 Ollama 和远程 API 两种提供者均支持打断
- [x] 工具调用循环中的打断也正常工作
- [x] 两个项目编译通过无错误
