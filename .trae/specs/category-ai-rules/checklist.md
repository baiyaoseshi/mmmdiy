# Checklist

- [x] `TaskCategoryData.AI规则` 字段存在且默认值为空字符串
- [x] `TaskCategoryViewModel.AI规则` 属性存在且默认值为空字符串
- [x] `MCP工具管理器.cs` 中 `对象型TaskCategory.AI规则` 字段存在，JSON 兼容
- [x] `MCPToolDefinition.分类AI规则` 字段存在
- [x] `MCPToolDefinition.祖先AI规则列表`（`List<string>`）字段存在
- [x] 顶级分类工具的 `分类AI规则` 正确填充，`祖先AI规则列表` 为空列表
- [x] 一级子分类工具的 `分类AI规则` 为子分类规则，`祖先AI规则列表` 含父分类规则
- [x] 多级子分类工具（如三层嵌套）的 `祖先AI规则列表` 按从根到父的顺序正确排列
- [x] 部分祖先无规则时，`祖先AI规则列表` 跳过空的
- [x] `收集分类规则提示词()` 按分类去重并生成格式化文本
- [x] 当前分类规则标注"必须遵守"，祖先规则列表标注"仅供参考，请提取关键要点理解"
- [x] 祖先规则以 `- 规则文本` 列表格式呈现（规则文本不含分类名前缀，实际可行）
- [x] Ollama 路径中分类规则提示词追加在工具描述之后
- [x] OpenAI 路径中分类规则作为额外 system 消息插入在通用工具指南之后
- [x] 所有分类及祖先均无规则时，两种路径均不注入分类规则
- [x] 分类右键菜单包含"编辑AI规则"选项（含子分类；"未分类"除外）
- [x] `EditCategoryAIRule()` 弹出输入框并保存规则
- [x] AI 规则在加载/保存时递归同步（`TaskCategoryData` ↔ `TaskCategoryViewModel`）
- [x] 对话设置面板显示已勾选分类的 AI 规则摘要
- [x] 无规则时规则摘要区域隐藏
- [x] 分类勾选变化时规则摘要实时刷新
- [x] 工具库项目编译通过
- [x] 用户界面项目编译通过
