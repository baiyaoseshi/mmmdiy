<Window x:Class="淼喵妙用户界面.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:淼喵妙用户界面.Controls"
        xmlns:converters="clr-namespace:淼喵妙用户界面.Converters"
        xmlns:viewModels="clr-namespace:淼喵妙用户界面.ViewModels" 
        xmlns:local="clr-namespace:淼喵妙用户界面"
        mc:Ignorable="d"
        Title="淼喵妙" Height="600" Width="800"
        Background="#FFE5E5E5"
        AllowDrop="True" Drop="Window_Drop"
        Closing="MainWindow_Closing"
        ShowInTaskbar="True">
    
    <Window.Resources>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <DataTemplate x:Key="ScriptPageTemplate" DataType="{x:Type viewModels:ScriptPageViewModel}">
            <controls:ScriptPageControl DataContext="{Binding}"/>
        </DataTemplate>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <DockPanel Grid.Row="0" Background="#FFA0A0A0" Margin="5" LastChildFill="True">
            <Button Content="+" Width="30" Height="28" Margin="2"
                    Click="AddNewPageButton_Click"
                    Background="#FF808080" Foreground="White"
                    DockPanel.Dock="Right"/>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button Content="主页" Width="60" Height="28" Margin="2"
                        Click="HomeButton_Click"
                        Background="{Binding IsHomePage, Converter={StaticResource InverseBooleanConverter}, ConverterParameter='#FF606060,#FF808080'}"
                        Foreground="White"/>
                <Separator Width="5"/>
                <ListBox ItemsSource="{Binding ScriptPages}"
                         SelectedItem="{Binding SelectedPage}"
                         Background="#FFA0A0A0"
                         BorderThickness="0"
                         Width="500"
                         SelectionMode="Single">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type viewModels:ScriptPageViewModel}">
                            <controls:TabHeaderControl DataContext="{Binding}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#FF606060"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </StackPanel>
        </DockPanel>
        
        <Grid Grid.Row="1" Margin="5">
            <controls:MainPageControl x:Name="MainPage" DataContext="{Binding}" Visibility="Visible"/>
            <controls:AIChatPage x:Name="AIPage" Visibility="Collapsed"/>
            <ContentControl x:Name="ScriptContent" Content="{Binding SelectedPage}" ContentTemplate="{StaticResource ScriptPageTemplate}" Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Window>