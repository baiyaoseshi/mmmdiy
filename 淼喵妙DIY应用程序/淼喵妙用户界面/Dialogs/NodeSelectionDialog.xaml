<Window x:Class="淼喵妙用户界面.Dialogs.NodeSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择节点" Height="500" Width="600"
        WindowStyle="ToolWindow" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Padding="5">
            <ItemsControl x:Name="CategoryItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,5,0,0">
                            <TextBlock Text="{Binding CategoryName}" 
                                       FontSize="14" FontWeight="Bold" 
                                       Foreground="#FF007ACC" Margin="0,5,0,3"/>
                            <ItemsControl ItemsSource="{Binding NodeTypes}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal" Width="570"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Content="{Binding DisplayName}" 
                                                Width="180" Height="35" 
                                                Margin="3" Padding="5"
                                                Background="#FFACACAC" 
                                                Foreground="White" 
                                                BorderBrush="#FF3C3C3C"
                                                BorderThickness="1"
                                                Click="NodeButton_Click"
                                                Tag="{Binding}"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="确定" Width="75" Height="25" Click="OKButton_Click"
                    IsEnabled="False" x:Name="OKButton">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#FFACACAC"/>
                        <Setter Property="Foreground" Value="#FF666666"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="Cursor" Value="Arrow"/>
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="True">
                                <Setter Property="Background" Value="#FF007ACC"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="取消" Width="75" Height="25" Click="CancelButton_Click"
                    Background="#FF3C3C3C" Foreground="White" BorderBrush="Transparent" Margin="5,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>